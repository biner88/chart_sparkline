name: chart_sparkline_example
description: A new Flutter application.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  chart_sparkline:
    path: ../
  flutter_hsvcolor_picker: "1.5.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
